import { TagRelatedDirective } from './tag-related.directive';

describe('TagRelatedDirective', () => {
  it('should create an instance', () => {
    const directive = new TagRelatedDirective();
    expect(directive).toBeTruthy();
  });
});
